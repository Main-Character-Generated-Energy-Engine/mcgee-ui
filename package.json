{
  "name": "mcgee-netlify",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:functions": "netlify dev --framework '#static' --dir web --port 8888 --no-open",
    "deploy": "npm test && flutter test && flutter build web --release && netlify deploy --prod --dir=build/web --functions=netlify/functions",
    "test": "node --test test/*_test.mjs"
  },
  "dependencies": {
    "@msgpack/msgpack": "^3.1.3",
    "ws": "^8.21.0"
  }
}
